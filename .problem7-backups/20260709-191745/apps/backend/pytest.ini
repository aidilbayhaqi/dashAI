[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = src/tests
markers =
    integration: tests that require live backend server
    static: tests that inspect app/routes without live server
    auth: authentication tests
    crud: CRUD endpoint tests
filterwarnings =
    ignore::DeprecationWarning